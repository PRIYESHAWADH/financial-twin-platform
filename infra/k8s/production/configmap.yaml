apiVersion: v1
kind: ConfigMap
metadata:
  name: fintwin-config
  namespace: fintwin-production
data:
  # Database Configuration
  DATABASE_URL: "postgresql://fintwin:password@postgres-service:5432/fintwin_production"
  REDIS_URL: "redis://redis-service:6379"
  
  # Application Configuration
  NODE_ENV: "production"
  PORT: "3001"
  FRONTEND_URL: "https://fintwin.in"
  API_URL: "https://api.fintwin.in"
  
  # ML Service Configuration
  ML_SERVICE_URL: "http://ml-service:8000"
  EMBEDDING_MODEL: "all-MiniLM-L6-v2"
  LLM_MODEL: "local-llama2"
  
  # Security Configuration
  JWT_SECRET: "production-jwt-secret-key"
  ENCRYPTION_KEY: "production-encryption-key"
  
  # External Services
  AA_SERVICE_URL: "https://aa.fintwin.in"
  ICAI_API_URL: "https://api.icai.org"
  GST_API_URL: "https://api.gst.gov.in"
  
  # Monitoring
  LOG_LEVEL: "warn"
  METRICS_ENABLED: "true"
  
  # Feature Flags
  ENABLE_AA_INTEGRATION: "true"
  ENABLE_ML_INSIGHTS: "true"
  ENABLE_CA_MARKETPLACE: "true"
  ENABLE_AUDIT_TRAIL: "true"
